/**
 * Single source of truth for UI components in the docs app.
 *
 * A hand-written re-export list stood here for 446 lines and had to be
 * edited every time a component was added. `@hex-core/components` is the
 * public API; mirroring it by hand only created a second thing to drift.
 */
export * from "@hex-core/components";
