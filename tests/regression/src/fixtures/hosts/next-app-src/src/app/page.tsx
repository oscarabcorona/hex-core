/** Index page — minimal scaffold. Tests rewrite this to import from
 * `@/components/ui/<slug>` after `hex add` to validate alias resolution. */
export default function HomePage() {
	return <main>regression host</main>;
}
