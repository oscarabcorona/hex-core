---
"@hex-core/motion": patch
---

Replace `track`'s one-line `accessibilityNotes` stub with a real note (structural label, reduced-motion behavior inherited from child Clips) — the single hard failure under the new `verify:schema-quality` gate.
